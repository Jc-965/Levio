name: motion_engine
description: Deterministic, camera-independent motion analysis for ParkiWell.
version: 0.2.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
